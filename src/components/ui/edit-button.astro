---
interface Props {
    file: string;
    githubRepo: string;
    githubBranch?: string;
}

const { file, githubRepo, githubBranch = "main" } = Astro.props;

const repoRelativePath = "src/" + file.split("/src/")[1];
const href = `https://github.com/${githubRepo}/blob/${githubBranch}/${repoRelativePath}`;
---

<a
    href={href}
    target="_blank"
    rel="noopener noreferrer"
    class="inline-flex items-center gap-1.5 text-sm text-surface-600-400 hover:text-surface-950-50 transition-colors"
>
    <svg xmlns="http://www.w3.org/2000/svg" class="size-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
        <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
    </svg>
    Edit on GitHub
</a>
